// given the recipe, pass associated token accounts of both 
// the caller (which possesses the ingredients) and the treasury
// if the consume method is "transfer"
// if the consume method of the ingredient is "burn", no need to pass the ATA of the treasury